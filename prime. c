#include<stdip.h>
int main()
{
  int n,i,count=0;
 printf("enter a number:");
scanf("%d",&n);
for(i=0;i<=n;i++)
{
 if(n%i==0)
   count++;
}
if(n%2==0)
  printf("prime number");
else
  printf("not prime number");
return 0;
}
  
